# Copy to terraform.tfvars and fill in the two values below.
# terraform.tfvars is gitignored — never committed.

subscription_id     = "your-azure-subscription-id"
entra_client_secret = "your-entra-client-secret"
